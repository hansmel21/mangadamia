# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

(This project is pinned to SDK 54 because the user's iPhone can only run an
Expo Go build that supports SDK 54. Do not upgrade the Expo SDK unless the
user confirms their Expo Go supports the newer SDK.)
