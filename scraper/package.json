{
  "name": "manga-scraper",
  "version": "1.0.0",
  "description": "Standalone scraper service — exposes the Source contract (MangaDex + Asura + Weeb Central) over HTTP for the main API to consume.",
  "main": "src/server.ts",
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=.env src/server.ts",
    "start": "tsx --env-file-if-exists=.env src/server.ts",
    "typecheck": "tsc",
    "test:source": "tsx --env-file-if-exists=.env scripts/test-source.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "cheerio": "^1.2.0",
    "fastify": "^5.10.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3"
  }
}
